import { Fragment } from "react";
import AvailableRecipes from "./AvailableRecipes";

const Recipes = () => {
  return (
    <Fragment>
      <AvailableRecipes />
    </Fragment>
  );
};

export default Recipes;
